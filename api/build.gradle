dependencies {
    implementation project(':core') // core 모듈 의존, 의존성 자체는 받지 못함.
    implementation 'org.springframework.boot:spring-boot-starter-web' // REST API
    implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.5.0'
}